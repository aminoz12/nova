[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "18"

# Simple catch-all for Next.js static
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
